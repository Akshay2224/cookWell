import React from 'react';
import "../styles/appointment.css"

function AppointmentCard({ date, time, doctor }) {
    return (
    
<div class="appointments-container">
  <div class="appointment-box">
    <h2>Add Recipe</h2>
  </div>
</div>

);
}

export default AppointmentCard;
